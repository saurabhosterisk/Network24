package com.network24.player.core.database.migration

// Phase 1: keep empty.
// Phase 2+: add Room Migration objects and expose them as an array.

object Migrations {
    // val ALL = arrayOf(MIGRATION_1_2, MIGRATION_2_3, ...)
}
